syntax = "proto3";

import "lararium/types.proto";

package lararium;

service Controller {
    rpc Login (types.LoginRequest) returns (types.LoginResponse);
}

service Adoption {
    rpc Propose (types.ProposeRequest) returns (types.ProposeResponse);
    rpc Accept (types.AcceptRequest) returns (types.AcceptResponse);
}
